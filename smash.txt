
if _G.Loaded == false then
	print("Stop Spamming Script Loaded")
	return
end
_G.Loaded = false

local success, err = pcall(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Venograde/Vino/main/menu.txt"))()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Venograde/Vino/main/Secret.txt"))()
end)

local players = game:GetService("Players")
local plr = players.LocalPlayer

_G.Aura = false
_G.Aura_Range =25

_G.kkall =false
_G.NoKnock = false

function Alive()
	local char = plr.Character or plr.Character.Parent

	if char then
		return char
	end
end 

function ClosestPlayer(range)
	local char = plr.Character

	local maxdist = range
	local target

	for i,v in pairs(game:GetService("Players"):GetChildren()) do
		if v.Name ~= plr.Name then
			local EChar = v.Character 

			if EChar then
				local ETorso = EChar:FindFirstChild("Torso")
				local EHuman = EChar:FindFirstChild("Humanoid")

				local distance = (char:FindFirstChild("Torso").Position - ETorso.Position).Magnitude
                local KnockPercent = EChar.Torso:FindFirstChild("percentIndicator").numForTweening

				if ETorso and EHuman and EHuman.Health >= 1 and not EChar:FindFirstChild("ForceField") and distance <= maxdist and KnockPercent.Value < 295 then
					target = v
				end
				
			end

		end
	end

	return target
end

function KnockBackAura(range)
	local success, err = pcall(function()
		local char = plr.Character
		local HRP = char:FindFirstChild("HumanoidRootPart")
		local EPlr = ClosestPlayer(range)

		if EPlr then
			local TPart = EPlr.Character:FindFirstChild("Torso")
			local THuman = EPlr.Character:FindFirstChild("Humanoid")

           	game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("koReflect"):FireServer(workspace:FindFirstChild("Living"):FindFirstChild(EPlr.Name),Vector3.new(-537.9108276367188, -12.901360511779785, -109.05764770507812),Vector3.zAxis)
		end
		
   	end)

end

function AllKnock()
	local success, err = pcall(function()
		for i,v in pairs(game:GetService("Players"):GetChildren()) do
			if v.Name ~= plr.Name then        
				local EChar = v.Character
                local TPart = EChar:FindFirstChild("Torso")

                local KnockPercent = TPart:FindFirstChild("percentIndicator").numForTweening

				if EChar and EChar:FindFirstChild("Humanoid") and EChar:FindFirstChild("Torso") and KnockPercent.Value < 295 then 
					game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("koReflect"):FireServer(workspace:FindFirstChild("Living"):FindFirstChild(v.Name),Vector3.new(-537.9108276367188, -12.901360511779785, -109.05764770507812),Vector3.zAxis)

				end

			end 
		end 
	end)
end 

function NoKnockback()
    local char =plr.Character

    if char then
        local HRP =char:FindFirstChild("HumanoidRootPart")

        if HRP:FindFirstChildOfClass("LinearVelocity") then
            HRP:FindFirstChildOfClass("LinearVelocity"):Remove()
        end   
    end

end 


local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()

local Window = OrionLib:MakeWindow({Name = "Project Smash(Update)", HidePremium = false, SaveConfig = false, ConfigFolder = "Orion"})

function Notfiy()

    OrionLib:MakeNotification({
        Name = "Orion Example",
        Content = "Orion Example",
        Image = "rbxassetid://4483345998",
        Time = 5
    })
end

local Combat = Window:MakeTab({
	Name = "Combat",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Misc = Window:MakeTab({
	Name = "Misc",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Info = Window:MakeTab({
	Name = "Info!!!📰",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Combat:AddToggle({
	Name = "Knockback-Aura",
	Default = false,
	Callback = function(value)
        if value then 
			_G.Aura = true
			while _G.Aura do 
                if Alive() then			
                    KnockBackAura(_G.Aura_Range)
                    task.wait()
                end
			end
		else
			_G.Aura = false
		end 
	end    
})

Combat:AddSlider({
	Name = "Aura-Range",
	Min = 10,
	Max = 50,
	Default = 20,
	Increment = 1,
	Callback = function(Value)
		_G.Aura_Range = Value
	end    
})

Combat:AddLabel("----------------------")

Combat:AddToggle({
	Name = "Knock-All",
	Default = false,
	Callback = function(value)
        if value then 
			_G.kkall = true
			while _G.kkall do 
                if Alive() then			
					AllKnock()
                    task.wait(0.05)
                end
			end
		else
			_G.kkall = false
		end 
	end    
})

-----COMBAT END -------

Misc:AddToggle({
	Name = "No-Knockback",
	Default = false,
	Callback = function(value)
        if value then 
			_G.NoKnock = true
			while _G.NoKnock do 
                if Alive() then			
					NoKnockback()
                    task.wait()
                end
			end
		else
			_G.NoKnock = false
		end 
	end    
})

-----MISC END -----------

Info:AddButton({
	Name = "Copy invite!",
	Callback = function()
        setclipboard("https://discord.gg/K6cNNkknQn")
        OrionLib:MakeNotification({
            Name = "Succes",
            Content = "Copied",
            Image = "rbxassetid://4483345998",
            Time = 5
        })
  	end    
})

Info:AddLabel("Join to my discord server! pls")
Info:AddLabel("Send bugs!")

Info:AddLabel("New features are coming in an hour!")

OrionLib:Init() --UI Lib End
